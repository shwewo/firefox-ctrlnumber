{
  "name": "firefox-ctrlnumber",
  "version": "1.1.0",
  "description": "Switch unpinned tabs with Ctrl+Number and pinned tabs with Alt+Shift+Number. Ctrl+9 and Alt+Shift+9 jump to the last tab in that group.",
  "main": "ctrlnumber.js",
  "scripts": {
    "test": "jsonlint --quiet *.json src/*.json && eslint . && web-ext lint --warnings-as-errors --no-input=true --source-dir src",
    "build": "web-ext build --overwrite-dest --no-input=true --source-dir src"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AbigailBuccaneer/firefox-ctrlnumber.git"
  },
  "author": "AbigailBuccaneer",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/AbigailBuccaneer/firefox-ctrlnumber/issues"
  },
  "homepage": "https://github.com/AbigailBuccaneer/firefox-ctrlnumber#readme",
  "devDependencies": {
    "eslint": "^8.53.0",
    "jsonlint": "^1.6.3",
    "web-ext": "^7.8.0"
  }
}
